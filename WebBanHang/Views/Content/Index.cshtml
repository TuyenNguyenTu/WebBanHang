@model List<Model.EntityF.Content>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h3 class="w3ls-title w3ls-title1">Danh sách nội dung </h3>
    @*<div class="w3ls-add-grids w3agile-add-products">
        <a href="#">
            <h4>TOP HOT <span>20%</span> OFF</h4>
            <h6>Shop now <i class="fa fa-arrow-circle-right" aria-hidden="true"></i></h6>
        </a>
    </div>*@
    <div class="offer-card-row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 offer-card-grids">
                <h4>@item.Name </h4>
                <p>@item.Decription</p>
                <a href="#">Shop Now <i class="fa fa-arrow-circle-right" aria-hidden="true"></i></a>
            </div>
        }

        <div class="clearfix"> </div>
    </div>
</div>

